<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="STONKS DEX 购物车 - 查看并结算您的订单">
    <meta name="keywords" content="STONKS DEX, $STONKS, 购物车, 结算">
    <title>STONKS DEX - 购物车</title>
    <link rel="stylesheet" href="https://stonksdex.shop/styles.css">
    <style>
        .cart-container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 1rem;
        }

        .cart-item {
            display: flex;
            gap: 1rem;
            padding: 1rem;
            background: #16213e;
            border-radius: 10px;
            margin-bottom: 1rem;
            color: white;
        }

        .cart-item img {
            max-width: 100px;
            border-radius: 5px;
        }

        .cart-item-info {
            flex: 1;
        }

        .cart-item-info h3 {
            margin-bottom: 0.5rem;
        }

        .cart-item-info p {
            margin: 0.3rem 0;
        }

        .cart-item button {
            background: #ff4d4d;
            color: white;
            padding: 0.3rem 0.6rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .cart-item button:hover {
            background: #e60000;
        }

        .cart-total {
            text-align: right;
            color: #00ffcc;
            font-size: 1.5rem;
            margin: 1rem 0;
        }

        .checkout-btn {
            background: #00ffcc;
            color: #1a1a2e;
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1.2rem;
            display: block;
            margin-left: auto;
        }

        .checkout-btn:hover {
            background: #00ccaa;
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">STONKS DEX <span class="powered">Powered by $STONKS</span></div>
        <nav>
            <ul>
                <li><a href="index.html">首页</a></li>
                <li><a href="products.html">产品</a></li>
                <li><a href="about.html">关于我们</a></li>
                <li><a href="manage.html">后台管理</a></li>
                <li><a href="https://t.me/STONKSOPEN">社区</a></li>
                <li><a href="cart.html">购物车 (<span id="cart-count">0</span>)</a></li>
            </ul>
        </nav>
    </header>

    <section class="cart-container">
        <h2>购物车</h2>
        <div id="cart-items"></div>
        <div class="cart-total" id="cart-total">总计：0 $STONKS</div>
        <button class="checkout-btn" id="checkout-btn">结算</button>
    </section>

    <footer>
        <div class="footer-content">
            <div class="footer-section">
                <h3>STONKS DEX</h3>
                <p>星际区域模块技术学院中欧区总用户，为加密货币投资者提供最佳策略和最新信息。</p>
                <div class="social-icons">
                    <a href="https://twitter.com/STONKSOPEN" target="_blank">🐦</a>
                    <a href="https://discord.gg/STONKSOPEN" target="_blank">💬</a>
                    <a href="https://t.me/STONKSOPEN" target="_blank">📢</a>
                    <a href="https://medium.com/@STONKSOPEN" target="_blank">📝</a>
                </div>
            </div>
            <div class="footer-section">
                <h3>快捷链接</h3>
                <ul>
                    <li><a href="#">区块资讯</a></li>
                    <li><a href="#">NFT 市场</a></li>
                    <li><a href="#">社区新闻</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>联系我们</h3>
                <p>📧 <a href="mailto:stonksdex-shop@gmail.com">stonksdex-shop@gmail.com</a></p>
                <p>📍 中国深圳区块链大厦 #42-01</p>
            </div>
            <div class="footer-section">
                <h3>订阅更新</h3>
                <p>订阅我们的通讯，了解最新产品和独家优惠信息。</p>
                <form class="subscribe-form">
                    <input type="email" placeholder="输入您的邮箱" required>
                    <button type="submit">📩</button>
                </form>
            </div>
        </div>
    </footer>

    <script>
        let cart = JSON.parse(localStorage.getItem('cart')) || [];

        // 渲染购物车
        function renderCart() {
            const cartItems = document.getElementById('cart-items');
            const cartTotal = document.getElementById('cart-total');
            const cartCount = document.getElementById('cart-count');
            cartItems.innerHTML = '';
            let total = 0;

            cart.forEach((item, index) => {
                total += item.price * item.quantity;
                const cartItem = document.createElement('div');
                cartItem.className = 'cart-item';
                cartItem.innerHTML = `
                    <img src="http://localhost:3000${item.imagePath}" alt="${item.name}">
                    <div class="cart-item-info">
                        <h3>${item.name}</h3>
                        <p>单价：${item.price} $STONKS</p>
                        <p>数量：${item.quantity}</p>
                    </div>
                    <button onclick="removeFromCart(${index})">移除</button>
                `;
                cartItems.appendChild(cartItem);
            });

            cartTotal.textContent = `总计：${total} $STONKS`;
            cartCount.textContent = cart.length;
        }

        // 移除商品
        function removeFromCart(index) {
            cart.splice(index, 1);
            localStorage.setItem('cart', JSON.stringify(cart));
            renderCart();
        }

        // 结算
        document.getElementById('checkout-btn').addEventListener('click', async () => {
            if (cart.length === 0) {
                alert('购物车为空！');
                return;
            }

            try {
                const response = await fetch('http://localhost:3000/api/orders', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ items: cart, total: cart.reduce((sum, item) => sum + item.price * item.quantity, 0) })
                });

                if (response.ok) {
                    alert('订单已提交！');
                    cart = [];
                    localStorage.setItem('cart', JSON.stringify(cart));
                    renderCart();
                } else {
                    alert('结算失败，请检查后端是否运行！');
                }
            } catch (error) {
                alert('无法连接到后端，请确保后端已启动！');
            }
        });

        renderCart();
    </script>
</body>
</html>