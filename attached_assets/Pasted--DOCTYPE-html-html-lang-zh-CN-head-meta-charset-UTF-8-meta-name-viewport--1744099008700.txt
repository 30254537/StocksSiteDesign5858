<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="STONKS DEX 周边产品 - 用 $STONKS 代币购买独家周边">
    <meta name="keywords" content="STONKS DEX, $STONKS, 去中心化交易所, 周边产品">
    <title>STONKS DEX - 产品</title>
    <link rel="stylesheet" href="https://stonksdex.shop/styles.css">
    <style>
        .search-filter {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 1rem;
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .search-filter input,
        .search-filter select {
            padding: 0.5rem;
            background: #1a1a2e;
            color: white;
            border: 1px solid #00ffcc;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">STONKS DEX <span class="powered">Powered by $STONKS</span></div>
        <nav>
            <ul>
                <li><a href="index.html">首页</a></li>
                <li><a href="products.html">产品</a></li>
                <li><a href="about.html">关于我们</a></li>
                <li><a href="manage.html">后台管理</a></li>
                <li><a href="https://t.me/STONKSOPEN">社区</a></li>
            </ul>
        </nav>
    </header>

    <section class="search-filter">
        <input type="text" id="search-input" placeholder="搜索产品名称">
        <select id="sort-select">
            <option value="price-asc">价格：低到高</option>
            <option value="price-desc">价格：高到低</option>
        </select>
    </section>

    <section class="products">
        <h2>STONKS DEX 周边产品</h2>
        <div class="product-grid" id="product-grid"></div>
    </section>

    <footer>
        <div class="footer-content">
            <div class="footer-section">
                <h3>STONKS DEX</h3>
                <p>星际区域模块技术学院中欧区总用户，为加密货币投资者提供最佳策略和最新信息。</p>
                <div class="social-icons">
                    <a href="https://twitter.com/STONKSOPEN" target="_blank">🐦</a>
                    <a href="https://discord.gg/STONKSOPEN" target="_blank">💬</a>
                    <a href="https://t.me/STONKSOPEN" target="_blank">📢</a>
                    <a href="https://medium.com/@STONKSOPEN" target="_blank">📝</a>
                </div>
            </div>
            <div class="footer-section">
                <h3>快捷链接</h3>
                <ul>
                    <li><a href="#">区块资讯</a></li>
                    <li><a href="#">NFT 市场</a></li>
                    <li><a href="#">社区新闻</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>联系我们</h3>
                <p>📧 <a href="mailto:stonksdex-shop@gmail.com">stonksdex-shop@gmail.com</a></p>
                <p>📍 中国深圳区块链大厦 #42-01</p>
            </div>
            <div class="footer-section">
                <h3>订阅更新</h3>
                <p>订阅我们的通讯，了解最新产品和独家优惠信息。</p>
                <form class="subscribe-form">
                    <input type="email" placeholder="输入您的邮箱" required>
                    <button type="submit">📩</button>
                </form>
            </div>
        </div>
    </footer>

    <script>
        let products = [];

        // 加载产品
        async function loadProducts() {
            try {
                const response = await fetch('http://localhost:3000/api/products');
                products = await response.json();
                renderProducts(products);
            } catch (error) {
                alert('无法加载产品列表，请确保后端已启动！');
            }
        }

        // 渲染产品
        function renderProducts(productsToRender) {
            const productGrid = document.getElementById('product-grid');
            productGrid.innerHTML = '';
            productsToRender.forEach(product => {
                const card = document.createElement('div');
                card.className = 'product-card';
                card.innerHTML = `
                    <a href="product-detail.html?id=${product.id}">
                        <img src="http://localhost:3000${product.imagePath}" alt="${product.name}">
                        <h3>${product.name}</h3>
                        <p>${product.price} $STONKS</p>
                        <span class="stonks-only">${product.stock > 0 ? '仅限 $STONKS' : '已售罄'}</span>
                    </a>
                `;
                productGrid.appendChild(card);
            });
        }

        // 搜索和筛选
        document.getElementById('search-input').addEventListener('input', (e) => {
            const searchTerm = e.target.value.toLowerCase();
            const filteredProducts = products.filter(product => 
                product.name.toLowerCase().includes(searchTerm)
            );
            renderProducts(filteredProducts);
        });

        document.getElementById('sort-select').addEventListener('change', (e) => {
            const sortValue = e.target.value;
            const sortedProducts = [...products];
            if (sortValue === 'price-asc') {
                sortedProducts.sort((a, b) => a.price - b.price);
            } else if (sortValue === 'price-desc') {
                sortedProducts.sort((a, b) => b.price - a.price);
            }
            renderProducts(sortedProducts);
        });

        loadProducts();
    </script>
</body>
</html>